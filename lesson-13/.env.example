# AWS Configuration
# Get your AWS Account ID: aws sts get-caller-identity --query Account --output text
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# ECR Repository Configuration
# This is the name of your ECR repository (will be created if it doesn't exist)
ECR_REPOSITORY_NAME=my-docker-app
IMAGE_NAME=my-app
IMAGE_TAG=v1.0.0

# AWS Credentials
# Option 1: Configure via AWS CLI (recommended)
#   Run: aws configure
#   This stores credentials in ~/.aws/credentials
#
# Option 2: Set environment variables (for CI/CD)
#   AWS_ACCESS_KEY_ID=your-access-key
#   AWS_SECRET_ACCESS_KEY=your-secret-key
#
# Option 3: Use IAM Role (for EC2/ECS/Lambda)
#   No credentials needed - AWS handles authentication automatically

# Notes:
# - Copy this file to .env and update with your values
# - Never commit .env to version control
# - Add .env to .gitignore
